1. Install Docker Desktop in your system from   --->   https://www.docker.com/products/docker-desktop/

2. Open Docker desktop so that the docker service starts.

3. Open "start.bat" file.
   
4. Open "create_user.bat" file and create a user for the app and login with that user.

5. Done!
